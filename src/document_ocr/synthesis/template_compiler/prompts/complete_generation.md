Generate the linguistic facts of ONE new fictional shipping scenario for a compiled bill-of-lading template. The host generates identifiers, dates, numeric quantities, masses, volumes and allocations. You do not edit OCR and must not change those structured facts. hostLexicalFacts, when present, also owns cargo descriptions and country-valid phone numbers. These are already reflected in structuredScenario. Do not regenerate them. If requested additional information jointly expresses a host-owned description, include that exact complete description, not a paraphrase or the old product name. Do not add unsupported material, formulation, grade, quality, certification or physical-property claims.

Return every requested field key, and no additional keys. Values are single-line semantic text, not whole documents or JSON strings. Several paths in one field are the SAME fact: produce one value for them. Preserve the meaning and scope of each field, not the original identity.

Generate genuinely new non-carrier organizations and postal addresses appropriate to the supplied cities/countries, business roles and goods. Use varied plausible fictional local business names, not a repetitive stock set of maritime names. Addresses must be new street/building identities in the fixed locality. Do not include the company name, contact details or document headings in an address. If a country/city/postal component is explicitly included in the source address or required projection frame, retain that structural component. Geographic fields not requested are fixed facts, not invitations to invent a new route.
If an address has segmented rendering with a final numeric-only postal slot, end the address value with a genuine locality-supported postal code of that required shape; print any separately requested city or country in its own field rather than appending it after that postal code. Never use `0000`, `00000`, `000000`, or a zero-filled country-prefixed code as a placeholder. Countries/localities without a postal-code system must not be assigned a fabricated code: use a postcode-free address only when the supplied slot contract permits it. If the required slot and sampled geography cannot both be satisfied, the candidate is incompatible and must fail instead of inventing a value.
The same rule applies to source-only postal facts and country/locality cargo marks. A mark such as `HK-00000 YUEN LONG` is invalid: preserve the meaningful country and locality components without fabricating a postal code, provided the marked slot can render that form. Do not erase an opaque identifier merely because it contains a run of zero digits.

An address field's expectedGeography is its immediate authority: do not substitute the issue city, loading/discharge port, another party's locality or another batch member's locality. If a locality appears in your address, use that party's exact supplied city and country. For removeLocalityComponents repairs, remove the conflicting locality rather than merely appending the requested city. Administrative-region names are distinct from the sampled postal locality.

When partyGeography is supplied, it is the authoritative NEW sampled geography for each party path, including parties whose training labels omit city/country. Retain the source address's component structure, but replace its OLD city/country/postal content to agree with the sampled geography. Do not preserve an old address country merely because it appeared in source text. hostAuxiliaryGeography contains host-owned source-only facts; do not regenerate or override them. Fixed literal/projection frames remain mandatory constraints. Phone country codes and local postal/region details must agree with the new party geography. Never infer a party's postal locality from its loading port.

Cargo descriptions must describe a new plausible commercial product/variant consistent with the supplied authoritative goods identity, HS codes, package types, masses, volumes, UN/hazard facts, equipment and temperatures. These may identify entirely NEW goods, not variants of the original product: replace all source product-specific content accordingly. For DG identities with properShippingName, include that complete name in the group's cargo description; vary fictional grade, producer or product detail without substituting another chemical or article. Preserve detail/style appropriate to the original: do not replace detailed merchandise by generic "general cargo". Do not copy the original description. Do not invent or change HS/UN codes or regulatory classifications. If a description includes quantity, weight, equipment, reference or temperature facts, use the NEW structured values exactly. Additional-information and handling fields must stay within their original semantic role. Pure generic marks/boilerplate may legitimately remain unchanged; product-, party-, identifier- and count-dependent text must be updated coherently. Formal package-number ranges must have the supplied required cardinality, not their source cardinality.

Rendering constraints are binding. Provide at least minimumWords where specified. MutableTokenIntervals identify the independently replaceable portions of one semantic value. Only fixedLiteralTokens are immutable between those portions: retain each such fragment exactly once in source order. For example, a description and repeated part number separated by fixed P/N must become a NEW description P/N NEW part number, never append a second part number to preserve the old one. If completeTokenPartition is provided, all address/text segments are mutable; do not retain old street/postal words merely because they had separate slots. Otherwise any fixedProjectionFrames still provided are required literal components. For an opaque-identifier field preserve its alphanumeric character classes and punctuation. Long repeated/multiline fields need enough words to populate all printed segments. Never create empty text to satisfy formatting. Never print internal schema enums such as PACKAGE_CARTON; print human package names such as CARTONS.

Auxiliary fields are contextual facts outside the training labels. Generate them coherently with the indicated newly generated party: postal/region data must match its address/locality, contact details its identity, and extension/registration fields their role. A country/region/postal fact may remain the same when geography is fixed. An auxiliary company/address must not retain the old identity if the corresponding party changed. Respect the supplied slot policies and source grammar.

Each goods identity with requiredDescription must appear in the group's description in full. This is the authoritative HS product definition, not a loose suggestion or permission to name a neighboring product. You may add compatible grade, material, manufacturer or finish details; never retain the old product just because it appeared in the template. For a registry definition listing several goods, the shipment may contain that assortment. Include every supplied identity for multi-product groups. Do not add incompatible goods or specialize an exclusion into the excluded article.

The sample ID is a diversity seed; choose an independently varied scenario for it. If prior validation errors are supplied, repair the exact incompatibilities while preserving all other requested changes. Never substitute the original party or cargo to avoid an error.
