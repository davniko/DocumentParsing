schema_version: 1
projection: mpci_bl_task_facing_packages_v1

source:
  dataset_id: mpci-bl-combined1157-current-policy-v1
  root: /mnt/d/Projects/DocumentParsing/artifacts/kie-training/datasets/mpci-bl-combined1157-current-policy-v1
  manifest_sha256: c43cbf2d4a8818ff7c174fe43c38bbd36db54ecaa8e172cbed5dacefaa7deaea
  expected_records: 1157

# Matching is exact after uppercase/punctuation/whitespace normalization. The
# values deliberately do not include "1000 KG IBC (STEEL PALLETS)": that is an
# IBC goods-package description, not a pallet fact.
policy:
  normalized_outer_types:
    - PALLET
    - PALLETS
    - PALLET S
    - PLT
    - PLTS
    - SKID
    - SKIDS
    - SHRINK WRAPPED PALLETS
    - HEAT TREATED PALLETS
    - PALLETS SLAC
    - PALLET SLAC
    - WOODEN PALLETS
    - 1000 KG PALLET WITH FILM
    - CTNR
  normalized_generic_types:
    - PACKAGE
    - PACKAGES
    - PACKAGE S
    - PKG
    - PKGS

# This reviewed fact is row-linked in the raw OCR table: quantity 1 belongs to
# the used semi-trailer cargo, while 27 PCS belongs to the spare parts. The
# source prints no package type for quantity 1, so its type remains absent.
# "SEMI TRAILER" is cargo description and "UNPACKED AND UNPROTECTED" is a
# handling statement; neither is invented as the package type.
reviewed_role_overrides:
  - document_id: doc_04cf3e2e9c722891f79a2ae092aac34655f1148ab8c1c4d4d43d5bf05a9642e6
    group_id: g1
    package_id: p1
    expected_package_sha256: 7650e6cb7e8605989fe6ff7950f31b1b6c32f5182d06709fe4372abce972993b
    role: direct_goods
    rationale: >-
      Quantity 1 is explicitly row-linked to the semi-trailer cargo; no package
      type is printed, so the fact is retained without a type.

output:
  dataset_id: mpci-bl-combined1157-task-facing-packages-v2
  root: /mnt/d/Projects/DocumentParsing/artifacts/kie-training/datasets/mpci-bl-combined1157-task-facing-packages-v2
